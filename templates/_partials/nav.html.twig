<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{path('app_home')}}">Sym R bnb</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbarColor01">
			<ul class="navbar-nav me-auto">
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_home')}}">Home
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_ads')}}">Offres</a>
				</li>
				<li class="nav-item">
					<a class="nav-link" href="{{path('app_contact')}}">Contact</a>
				</li>
			</ul>
			{% if app.user %}
			
			<div class="d-flex">

			{% if is_granted('ROLE_ADMIN') %}
			<a href="{{path('admin')}}" class="btn btn-outline-secondary btn-sm me-lg-3 me-3 text-white">Dashboard</a>
			
			{% endif %}
			
				<a href="{{path('app_account')}}" class="btn btn-outline-secondary btn-sm me-lg-3 me-3 text-white">Mon compte</a>
				<a href="{{path('app_logout')}}" class="btn btn-outline-secondary btn-sm me-lg-3 me-3 text-white">Logout</a>
			</div>
			{% else %}
			<div class="d-flex">
				<a href="{{path('app_login')}}" class="btn btn-outline-secondary btn-sm me-lg-3 me-3 text-white">Connexion</a>
				<a href="{{path('app_register')}}" class="btn btn-outline-secondary btn-sm me-lg-3 me-3 text-white">S'inscrire</a>
			</div>
			{% endif %}
		</div>
	</div>
</nav>
